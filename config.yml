server:
    address: 127.0.0.1
    port: 8080
    https: false # Whether or not the returning url should be HTTPS or not

files:
    filesPath: /files
    maxUploadSize: 20
    keyLength: 7
    password: "changeMe!" # Password required for uploading, leave empty for no password
    allowedFileTypes:
        [
            .png,
            .jpg,
            .gif,
            .mp4,
            .mp3,
            .jpeg,
            .tiff,
            .bmp,
            .ico,
            .psd,
            .eps,
            .raw,
            .cr2,
            .nef,
            .sr2,
            .orf,
            .svg,
            .wav,
            .webm,
            .aac,
            .flac,
            .ogg,
            .wma,
            .m4a,
            .gifv,
        ]

other:
    prettyOutput: true
